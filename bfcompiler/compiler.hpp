#pragma once

#include <vector>
#include <string>

class instruction {
	size_t ptr;
	size_t 
};

class compiler {
private:
	std::vector<char> prg;

public:
	compiler ();
	void add_prg(std::string);
};
